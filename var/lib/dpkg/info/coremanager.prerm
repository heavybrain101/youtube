#!/bin/sh
set -e
# Automatically added by dh_installinit/12.10ubuntu1
if [ -x "/etc/init.d/ihttpd" ] && [ "$1" = remove ]; then
	invoke-rc.d ihttpd stop || exit 1
fi
# End automatically added section
